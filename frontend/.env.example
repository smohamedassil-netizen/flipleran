# URL du backend pour Socket.io
# Dev   : http://localhost:5000
# Prod  : https://fliplearn-5lsz.onrender.com (ou ton URL Render)
VITE_SOCKET_URL=http://localhost:5000

# URL de base de l'API
# Dev   : laisser vide → proxy Vite vers localhost:5000
# Prod  : laisser vide aussi (déploiement monolithique Render — Express sert
#         le frontend buildé et /api/* sur la même origine, axios utilise
#         des chemins relatifs)
# VITE_API_URL=
