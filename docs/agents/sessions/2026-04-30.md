# Session — 2026-04-30

**Type** : double sprint (pédagogie P2-P7 + sprint final F1-F5)

## Volume

- **14 commits** sur `sprint-final` + `sprint-pedagogy/*`, tous mergés et poussés sur `main`.
- 11 940 insertions, 115 deletions, 48 fichiers créés, 20 modifiés.
- Branche `main` à `c1440a8` à la fin de session, working tree clean.

## Sprint pédagogie (refonte du plan PFE) — branche `sprint-0-security`

| Code | Sujet |
|---|---|
| **P2** | Parcours scénarisé : `LearningPath` + `LearningPathBuilder` + Timeline frontend |
| **P3** | Peer assessment 70% prof + 30% pairs (Falchikov 2005, Topping 1998) |
| **P4** | Objectifs de Bloom (Anderson & Krathwohl 2001) + contrat pédagogique Markdown par cours |
| **P5** | Fiche méthode CESI in-app + onboarding modal premier login |
| **P6** | 5 diagrammes Mermaid pour le mémoire (`docs/diagrams/`) |
| **P7** | `backend/scripts/seed-soutenance.js` + `docs/script-soutenance.md` (démo 7 min) |

## Sprint final (plan PLAN-FLIPLEARN-PROMPTS-CLAUDE-CODE) — branche `sprint-final`

| Code | Sujet | Commit |
|---|---|---|
| **F1** | Auto-prep cours en 1 clic (Groq × 5 parallèle) + hotfix prefill PrositCreate | `4210d9a` + `9d654fa` |
| **F2** | Détection plagiat IA (Mitchell 2023, DetectGPT) + composer de groupes Prosit | `16eec0d` + `110b0f9` |
| **F3** | Insights pédagogiques IA (Hattie 2009) + suggestions étudiants individualisées | `f7379c3` |
| **F4** | Tuteur IA personnel `/my-tutor` + bulle flottante TutorBubble + Web Speech API | `cc5d128` |
| **F5** | Ask-the-video — RAG transcript + timestamps cliquables qui seek la vidéo | `e9718fc` |

## Comptes de démo (seed-soutenance)

- **Prof** : `lebrun@fliplearn.dz` / `demo1234`
- **Étudiants** : `sara.demo`, `yanis.demo`, `lina.demo`, `walid.demo`, `rania.demo`, `samir.demo`, `imane.demo`, `mehdi.demo` (suffixe `@fliplearn.dz`, password `demo1234`)
- **Reset** : `node backend/scripts/seed-soutenance.js --reset`

## À reprendre — F6 (auto-flashcards SM-2)

Plan dans `PLAN-FLIPLEARN-PROMPTS-CLAUDE-CODE.md` (hors repo, dans `OneDrive/Documents/`).

Étapes prévues :
1. `git checkout sprint-final && git pull`
2. Créer `backend/services/autoFlashcards.js` + endpoint `POST /api/decks/auto-generate`
3. Cron dimanche 09:00 pour regen hebdo
4. Frontend :
   - Section "Tes decks de révision auto-générés" sur `Decks.jsx`
   - Widget dashboard "X cartes à réviser aujourd'hui"
   - Toast quand vidéo complétée à 100%

**Modèles déjà en place** :
- `backend/models/Deck.js` — owner, title, description, category, isPublic, tags, cardCount
- `backend/models/Card.js` — deck (ref), front, back, difficulty (easy/medium/hard), nextReview, interval, easeFactor, repetitions

Le mapping difficulty IA 1-5 → enum easy/medium/hard est déjà dans `backend/services/courseAutoPrep.js` (`publishResults`).

## Règle d'or de la session

**Un prompt à la fois** : F1 → test → commit → merge → F2 → test → commit → merge → … → F5.
L'auteur a confirmé "passe à la suite" entre chaque sprint. À continuer pareil pour F6 → F15.
